export interface DropListPropsType {
  // eslint-disable-next-line no-unused-vars, @typescript-eslint/no-explicit-any
  dropdownList: Array<{ text: string, onClick: (e?: any) => void }>;
  isActive: boolean;
}